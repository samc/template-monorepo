/**
 * State of the active code environment, paralleled by
 * defaults for `process.env.NODE_ENV`
 */
export enum Stage {
	DEVELOPMENT = "development",
	STAGING = "staging",
	PRODUCTION = "production",
}
