/**
 * Represents the type of client being used. Usually used in tandem with
 * breakpoints to identify relative styling.
 */
export enum Device {
	MOBILE = "MOBILE",
	TABLET = "TABLET",
	DESKTOP = "DESKTOP",
	ULTRAWIDE = "ULTRAWIDE",
}
