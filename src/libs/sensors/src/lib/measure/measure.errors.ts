export enum Panic {
	TARGET_ELEMENT_NOT_FOUND = "Target element not found. Please pass a valid React ref to the <Measure> sensor component via `target`.",
	USED_OUTSIDE_OF_PROVIDER = "This component must be used within the <Measure> sensor component.",
}

export enum Warn {}
