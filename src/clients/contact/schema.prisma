datasource db {
  provider = "sqlite"
  url      = "file:dev.db"
}

generator graphql {
  provider = "graphql-schema-generator"
  output   = "./graphql"
  createCRUD = "true"
}

model Contact {
  id String @id @default(cuid())
  email String @unique
  name String
  message String
}

