{
  "name": "@taygo/function.contact",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "clean": "run-s \"clean:*\"",
    "clean:api": "trash api/",
    "clean:buf": "trash buf.lock",
    "clean:go": "trash go.sum",
    "clean:turbo": "trash .turbo/",
    "dev": "go run ./cmd/app",
    "build": "go build ./cmd/app",
    "format": "run-s \"format:*\"",
    "format:prisma": "prisma format",
    "generate": "run-s \"generate:*\" update:go",
    "generate:pb": "run-s \"generate:pb:*\"",
    "generate:pb:source": "buf export buf.build/taygo/contact --output ./api",
    "generate:pb:go": "buf generate buf.build/taygo/contact --include-imports --template=./.buf/buf.go.yaml",
    "generate:pb:graphql": "buf generate buf.build/taygo/contact --include-imports --template=./.buf/buf.graphql.yaml",
    "generate:pb:schema": "zx ./scripts/schema.mjs",
    "generate:templates": "run-s templates:update format:prisma",
    "generate:prisma": "prisma db push; prisma generate",
    "generate:graphql": "gqlgen generate",
    "generate:go": "go generate ./...",
    "init": "run-s \"init:*\" update generate",
    "init:schema": "mkdir -p ./api/ent/schema",
    "reset": "run-s clean init generate",
    "start": "npm run dev",
    "templates:update": "hygen function update --name=contact",
    "update": "run-s \"update:*\"",
    "update:buf": "buf mod update",
    "update:prisma": "prisma",
    "update:go": "go mod tidy -compat=1.17"
  },
  "devDependencies": {
    "@apidevtools/swagger-parser": "^10.0.3",
    "@grpc/grpc-js": "^1.5.6",
    "@grpc/proto-loader": "^0.6.9",
    "@prisma-korea/graphql-schema-generator": "^0.3.0",
    "google-protobuf": "^3.19.4",
    "grpc": "^1.24.11",
    "hygen": "^6.1.0",
    "npm-run-all": "^4.1.5",
    "prisma": "^3.9.2",
    "protobufjs": "^6.11.2",
    "tiny-glob": "^0.2.9",
    "trash-cli": "^5.0.0",
    "zx": "^5.1.0"
  }
}
