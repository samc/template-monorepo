# Generated with protoc-gen-openapi
# https://github.com/google/gnostic/tree/master/apps/protoc-gen-openapi

openapi: 3.0.3
info:
    title: AccountService API
    version: 0.0.1
paths:
    /v1/accounts:
        put:
            tags:
                - AccountService
            description: |-
                Get takes a request `UpdateAccountRequest` and returns a response
                 `Account`
            operationId: AccountService_Update
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/Account'
                required: true
            responses:
                "200":
                    description: OK
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Account'
        post:
            tags:
                - AccountService
            description: |-
                Create takes a request `CreateAccountRequest` and returns a response
                 `Account`
            operationId: AccountService_Create
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/Account'
                required: true
            responses:
                "200":
                    description: OK
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Account'
    /v1/accounts/{account.id}:
        get:
            tags:
                - AccountService
            description: |-
                Get takes a request `GetAccountRequest` and returns a response
                 `Account`
            operationId: AccountService_Get
            parameters:
                - name: account.id
                  in: path
                  required: true
                  schema:
                    type: string
                - name: account.id
                  in: query
                  description: The id of the `Account` entity.
                  schema:
                    type: string
                - name: account.email
                  in: query
                  description: The email of the `Account` entity.
                  schema:
                    type: string
                - name: account.credential.id
                  in: query
                  description: The id of the OAuth2 credential entity.
                  schema:
                    type: string
                - name: account.credential.account.id
                  in: query
                  description: The id of the `Account` entity.
                  schema:
                    type: string
                - name: account.credential.account.email
                  in: query
                  description: The email of the `Account` entity.
                  schema:
                    type: string
                - name: account.credential.account.credential.id
                  in: query
                  description: The id of the OAuth2 credential entity.
                  schema:
                    type: string
                - name: account.credential.account.credential.basic.id
                  in: query
                  description: The id for the basic credential entity.
                  schema:
                    type: string
                - name: account.credential.account.credential.basic.credential.basic.id
                  in: query
                  description: The id for the basic credential entity.
                  schema:
                    type: string
                - name: account.credential.account.credential.basic.credential.basic.credential.oauth2.id
                  in: query
                  description: The id of the OAuth2 credential entity.
                  schema:
                    type: string
                - name: account.credential.account.credential.basic.credential.basic.credential.oauth2.credential.oauth2.id
                  in: query
                  description: The id of the OAuth2 credential entity.
                  schema:
                    type: string
                - name: account.credential.account.credential.basic.credential.basic.credential.oauth2.credential.oauth2.credential.type
                  in: query
                  description: Indicates the type of credential, available via the `credential` field.
                  schema:
                    type: integer
                    format: enum
                - name: account.credential.account.credential.basic.credential.basic.credential.oauth2.credential.oauth2.access
                  in: query
                  description: The access token of the OAuth2 authorization entity.
                  schema:
                    type: string
                - name: account.credential.account.credential.basic.credential.basic.credential.oauth2.credential.oauth2.refresh
                  in: query
                  description: The refresh token of the OAuth2 authorization entity.
                  schema:
                    type: string
                - name: account.credential.account.credential.basic.credential.basic.credential.oauth2.credential.type
                  in: query
                  description: Indicates the type of credential, available via the `credential` field.
                  schema:
                    type: integer
                    format: enum
                - name: account.credential.account.credential.basic.credential.basic.credential.oauth2.access
                  in: query
                  description: The access token of the OAuth2 authorization entity.
                  schema:
                    type: string
                - name: account.credential.account.credential.basic.credential.basic.credential.oauth2.refresh
                  in: query
                  description: The refresh token of the OAuth2 authorization entity.
                  schema:
                    type: string
                - name: account.credential.account.credential.basic.credential.basic.username
                  in: query
                  description: The username of the parent `Account` entity.
                  schema:
                    type: string
                - name: account.credential.account.credential.basic.credential.basic.hash
                  in: query
                  description: A Salted hash of the parent `Account` entity's password.
                  schema:
                    type: string
                - name: account.credential.account.credential.basic.username
                  in: query
                  description: The username of the parent `Account` entity.
                  schema:
                    type: string
                - name: account.credential.account.credential.basic.hash
                  in: query
                  description: A Salted hash of the parent `Account` entity's password.
                  schema:
                    type: string
            responses:
                "200":
                    description: OK
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Account'
        delete:
            tags:
                - AccountService
            description: |-
                Delete takes a request `DeleteAccountRequest` and returns a response
                 `google.protobuf.Empty`
            operationId: AccountService_Delete
            parameters:
                - name: account.id
                  in: path
                  required: true
                  schema:
                    type: string
                - name: account.id
                  in: query
                  description: The id of the `Account` entity.
                  schema:
                    type: string
                - name: account.email
                  in: query
                  description: The email of the `Account` entity.
                  schema:
                    type: string
                - name: account.credential.id
                  in: query
                  description: The id of the OAuth2 credential entity.
                  schema:
                    type: string
                - name: account.credential.account.id
                  in: query
                  description: The id of the `Account` entity.
                  schema:
                    type: string
                - name: account.credential.account.email
                  in: query
                  description: The email of the `Account` entity.
                  schema:
                    type: string
                - name: account.credential.account.credential.id
                  in: query
                  description: The id of the OAuth2 credential entity.
                  schema:
                    type: string
                - name: account.credential.account.credential.basic.id
                  in: query
                  description: The id for the basic credential entity.
                  schema:
                    type: string
                - name: account.credential.account.credential.basic.credential.basic.id
                  in: query
                  description: The id for the basic credential entity.
                  schema:
                    type: string
                - name: account.credential.account.credential.basic.credential.basic.credential.oauth2.id
                  in: query
                  description: The id of the OAuth2 credential entity.
                  schema:
                    type: string
                - name: account.credential.account.credential.basic.credential.basic.credential.oauth2.credential.oauth2.id
                  in: query
                  description: The id of the OAuth2 credential entity.
                  schema:
                    type: string
                - name: account.credential.account.credential.basic.credential.basic.credential.oauth2.credential.oauth2.credential.type
                  in: query
                  description: Indicates the type of credential, available via the `credential` field.
                  schema:
                    type: integer
                    format: enum
                - name: account.credential.account.credential.basic.credential.basic.credential.oauth2.credential.oauth2.access
                  in: query
                  description: The access token of the OAuth2 authorization entity.
                  schema:
                    type: string
                - name: account.credential.account.credential.basic.credential.basic.credential.oauth2.credential.oauth2.refresh
                  in: query
                  description: The refresh token of the OAuth2 authorization entity.
                  schema:
                    type: string
                - name: account.credential.account.credential.basic.credential.basic.credential.oauth2.credential.type
                  in: query
                  description: Indicates the type of credential, available via the `credential` field.
                  schema:
                    type: integer
                    format: enum
                - name: account.credential.account.credential.basic.credential.basic.credential.oauth2.access
                  in: query
                  description: The access token of the OAuth2 authorization entity.
                  schema:
                    type: string
                - name: account.credential.account.credential.basic.credential.basic.credential.oauth2.refresh
                  in: query
                  description: The refresh token of the OAuth2 authorization entity.
                  schema:
                    type: string
                - name: account.credential.account.credential.basic.credential.basic.username
                  in: query
                  description: The username of the parent `Account` entity.
                  schema:
                    type: string
                - name: account.credential.account.credential.basic.credential.basic.hash
                  in: query
                  description: A Salted hash of the parent `Account` entity's password.
                  schema:
                    type: string
                - name: account.credential.account.credential.basic.username
                  in: query
                  description: The username of the parent `Account` entity.
                  schema:
                    type: string
                - name: account.credential.account.credential.basic.hash
                  in: query
                  description: A Salted hash of the parent `Account` entity's password.
                  schema:
                    type: string
            responses:
                "200":
                    description: OK
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Account'
components:
    schemas:
        Account:
            type: object
            properties:
                id:
                    type: string
                    description: The id of the `Account` entity.
                email:
                    type: string
                    description: The email of the `Account` entity.
                credential:
                    $ref: '#/components/schemas/Credential'
        Credential:
            type: object
            properties:
                id:
                    type: string
                    description: The id of the OAuth2 credential entity.
                account:
                    $ref: '#/components/schemas/Account'
                basic:
                    $ref: '#/components/schemas/CredentialBasic'
                oauth2:
                    $ref: '#/components/schemas/CredentialOAuth2'
                type:
                    type: integer
                    description: Indicates the type of credential, available via the `credential` field.
                    format: enum
            description: '...'
        CredentialBasic:
            type: object
            properties:
                id:
                    type: string
                    description: The id for the basic credential entity.
                credential:
                    $ref: '#/components/schemas/Credential'
                username:
                    type: string
                    description: The username of the parent `Account` entity.
                hash:
                    type: string
                    description: A Salted hash of the parent `Account` entity's password.
            description: '...'
        CredentialOAuth2:
            type: object
            properties:
                id:
                    type: string
                    description: The id of the OAuth2 credential entity.
                credential:
                    $ref: '#/components/schemas/Credential'
                access:
                    type: string
                    description: The access token of the OAuth2 authorization entity.
                refresh:
                    type: string
                    description: The refresh token of the OAuth2 authorization entity.
            description: '...'
tags:
    - name: AccountService
