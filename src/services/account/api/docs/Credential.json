{
  "title": "Credential",
  "$id": "Credential.json",
  "$schema": "http://json-schema.org/draft-07/schema#",
  "type": "object",
  "description": "...",
  "properties": {
    "id": {
      "title": "id",
      "type": "string",
      "description": "The id of the OAuth2 credential entity."
    },
    "account": {
      "description": "The parent `Account` entity.",
      "$ref": "Account.json"
    },
    "basic": {
      "$ref": "CredentialBasic.json"
    },
    "oauth2": {
      "$ref": "CredentialOAuth2.json"
    },
    "type": {
      "title": "type",
      "type": "integer",
      "description": "Indicates the type of credential, available via the `credential` field.",
      "format": "enum"
    }
  }
}
