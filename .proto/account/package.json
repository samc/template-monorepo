{
  "name": "@eden/proto.account",
  "version": "0.0.0",
  "private": true,
  "files": [
    "buf.lock",
    "buf.yaml",
    "eden"
  ],
  "scripts": {
    "update": "run-s \"update:*\"",
    "update:modules": "buf mod update",
    "update:publish": "buf push"
  },
  "dependencies": {
    "@eden/proto.api": "^0.0.0",
    "@eden/proto.prisma": "^0.0.0"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5",
    "trash-cli": "^5.0.0"
  }
}
