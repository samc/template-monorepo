apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "function.fullname" . }}-test-connection"
  labels:
    {{- include "function.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "function.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
